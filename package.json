{
  "scripts": {
    "build": "typings bundle . -o bundle.d.ts",
    "test": "ts-node test.ts"
  },
  "private": true,
  "devDependencies": {
    "blue-tape": "^0.2.0",
    "ts-node": "^0.5.5",
    "tslint": "^3.5.0",
    "typescript": "^1.8.2"
  }
}
